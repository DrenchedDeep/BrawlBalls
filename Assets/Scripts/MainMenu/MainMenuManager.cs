using UnityEngine;
using Utilities;

namespace MainMenu
{
    [DefaultExecutionOrder(-1000)]
    public class MainMenuManager : MonoBehaviour
    {
        private void Awake()
        {
            Debug.LogWarning("Load and spawn the balls");
        }

    }
}
